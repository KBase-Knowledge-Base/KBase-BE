# M11 verification-only override. A long poll interval keeps new durable jobs
# pending until the backend is restarted with the normal runtime-test override.
# It is not a deployment configuration.
services:
  backend:
    environment:
      KBASE_AI_WORKER_POLL_INTERVAL: 10m

# Tracker Nợ Kỹ thuật

Sử dụng tệp này cho nợ thực, đã được thừa nhận và có chủ ý bị hoãn.

| Ngày | Khu vực | Nợ | Lý do Hoãn | Rủi ro | Kích hoạt Tiếp theo |
|------|------|------|--------------|------|--------------| 
| 2026-09-17 | Database docs | `docs/generated/db-schema.md` được tái sinh thủ công trong cùng thay đổi migration thay vì bằng generator tự động | M2 chỉ tạo schema, chưa có JPA entity/runtime để chạy generator schema tự động | Docs có thể lệch nếu migration đổi mà quên tái sinh | Milestone JPA (M3) khi thiết lập generator hoặc quy trình sinh schema |
| 2026-09-17 | Testing | Hibernate `ddl-auto=validate` trong M2 chạy với persistence surface chưa có entity — **RESOLVED 2026-09-17**: M3/JPA-11 đã chạy validate với đầy đủ entity trên PostgreSQL Testcontainer | Thứ tự milestone theo master plan: schema trước, mapping sau | Drift Flyway/JPA hiện đã được kiểm tra bằng mapping integration suite | M3/JPA-11 đã hoàn tất; theo dõi regression ở các thay đổi persistence sau |
| 2026-09-17 | Testing | `OTP_SERVICE_UNAVAILABLE` (503) cho auth API mới verify ở adapter level (`RedisOtpStoreIntegrationTest`) và unit level (`EmailVerificationServiceTest` propagate); chưa có full-context auth test với Redis chết | Tránh khởi động thêm một `@SpringBootTest` context (PostgreSQL+Redis containers) cho cùng error mapping đã chứng minh; tăng thời gian suite đáng kể | Nếu mapping giữa `OtpStore`/`OtpService` và auth flow thay đổi, 503 contract có thể lệch mà test không bắt được | Khi M7+ chạm lại auth error mapping hoặc khi có cơ chế shared/context reuse cho testcontainers |
| 2026-09-17 | Docs | `docs/generated/api-schema.md` phần auth endpoints (M6) được đồng bộ thủ công từ source code đã verify; generator springdoc runtime vẫn chưa thiết lập — **RESOLVED 2026-09-18**: M13 đã bật springdoc runtime; `/v3/api-docs` là contract máy đọc được và 21 OpenAPI contract tests chặn drift | OpenAPI runtime generation thuộc M13 theo master plan | Đã resolve; runtime spec được verify tự động | Đã hoàn tất tại M13 |
| 2026-09-18 | Docs | Chi tiết field-level trong `docs/generated/api-schema.md` vẫn đồng bộ thủ công từ runtime `/v3/api-docs` (Markdown snapshot); contract tests chỉ chặn drift ở mức security/multipart/binary/error-code | Không có generator Markdown tự động; runtime spec đã là contract máy đọc được nên rủi ro chỉ còn ở layer snapshot đọc của agent | Markdown snapshot có thể lệch nếu thay đổi API mà quên sync | Khi thay đổi API contract tiếp theo hoặc khi thiết lập generator/snapshot CI |
| 2026-09-18 | Verification | Swagger UI mới được verify qua MockMvc (redirect + api-docs 200 với real filter chain); chưa smoke Swagger UI qua browser thật — **RESOLVED 2026-09-18**: M14 đã verify Swagger UI redirect + OpenAPI 200 trong container runtime; UI browser tương tác thực tế là UX-only | Browser smoke thuộc full Docker runtime verification (M14) | Đã resolve ở mức runtime contract | Đã hoàn tất tại M14 |
| 2026-09-18 | Integration | Gmail SMTP delivery thật (App Password thật) chưa chạy; automated Docker runtime smoke dùng mail double (`docker-compose.mail-test.yml`, mailpit) tại MailService boundary | Credential Gmail thật không có trong repo/CI; automated path đã chứng minh đến boundary theo SD-12 | Nếu Gmail config sai ở production, gửi mail thật có thể fail dù automated pass | Khi có Gmail credential được phê duyệt cho manual/production smoke |
| 2026-09-18 | Operations | Docker runtime smoke là chuỗi lệnh thủ công ghi trong `docs/DEVELOPMENT.md`, chưa script hóa thành một lệnh duy nhất — M15 đã chạy lại toàn bộ smoke thủ công này thành công (2026-09-19) | Script bổ sung sẽ là code cần bảo trì; các bước đã ghi đủ chi tiết để lặp lại và đã được chạy lặp lại thành công hai lần liên tiếp (M14, M15) | Smoke có thể bỏ sót bước nếu chạy tay | Khi cần chạy smoke định kỳ (CI/scheduled) hoặc trước một phase mới sau freeze |
| YYYY-MM-DD | `[khu vực]` | `[nợ]` | `[lý do]` | `[rủi ro]` | `[khi nào xem xét lại]` |

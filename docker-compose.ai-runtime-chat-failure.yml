# M11 verification-only override. Combine with docker-compose.ai-runtime-test.yml
# to fail chat after successful embedding/retrieval; never use for deployment.
services:
  backend:
    environment:
      KBASE_AI_DETERMINISTIC_FAILURE_MODE: chat_unavailable
